services:
  backend:
    build: ./backend
    volumes:
      - ./backend/app:/app/app       # hot-reload en dev
      - ./data:/data                  # persistencia SQLite
      - ./data/static:/app/static     # persistencia de imágenes generadas
    env_file: .env
    command: >
      uvicorn app.main:app
      --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"                   # expuesto en dev para debugging
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      llama-cpp:
        condition: service_started
        required: false

  llama-cpp:
    build: ./llama-cpp
    volumes:
      - ${MODELS_DIR:-./models}:/models
    environment:
      MODEL_PATH: /models/${LLAMA_MODEL_FILE:-Qwen3.5-4B-Q4_K_M.gguf}
      LLAMA_THREADS: ${LLAMA_THREADS:-8}
      LLAMA_ARG_THINK_BUDGET: ${LLAMA_THINK_BUDGET:-128}

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
