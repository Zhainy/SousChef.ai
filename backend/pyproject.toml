[project]
name = "souschef-backend"
version = "1.0.0"
description = "Backend de SousChef.ai: despensa + agente IA con Tool Calling"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "sqlmodel",
    "pydantic-settings",
    "httpx",
    "oci-openai==1.1.0",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.fastapi]
entrypoint = "app.main:app"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
